print "==> Give me a positive integer: "
fir = gets.to_i
print "==> Give me one more positive integer: "
sec = gets.to_i

puts "==> #{fir} + #{sec} = #{fir + sec}"
puts "==> #{fir} - #{sec} = #{fir - sec}"
puts "==> #{fir} * #{sec} = #{fir * sec}"
puts "==> #{fir} / #{sec} = #{fir / sec}"
puts "==> #{fir} % #{sec} = #{fir % sec}"
puts "==> #{fir} ** #{sec} = #{fir**sec}"
